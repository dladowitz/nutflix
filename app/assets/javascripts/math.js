function add(x, y){
  x = parseFloat(prompt("Give me a number:"));
  y = parseFloat(prompt("Well I can't add just one number can I?"));

  alert(x + y);
}

function subtract(x, y){
  x = parseFloat(prompt("Give me a number:"));
  y = parseFloat(prompt("Well I can't add just one number can I?"));

  alert(x - y);
}

function multiply(x, y){
  x = parseFloat(prompt("Give me a number:"));
  y = parseFloat(prompt("Well I can't add just one number can I?"));

  alert(x * y);
}

function divide(x, y){
  x = parseFloat(prompt("Give me a number:"));
  y = parseFloat(prompt("Well I can't add just one number can I?"));

  alert(x / y);
}
